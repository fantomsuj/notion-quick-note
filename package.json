{
  "name": "notion-quick-note",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/background.js && node --check src/content.js && node --check options/options.js && node --test",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
