// These values are public product configuration, not secrets. Set them before
// packaging the Chrome Web Store build. Local developers can override them from
// Advanced setup on the options page.
export const PRODUCT_CONFIG = Object.freeze({
  notionClientId: "",
  oauthBrokerUrl: ""
});
